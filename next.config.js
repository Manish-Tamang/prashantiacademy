/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['img.icons8.com'], // Add the hostname of your images here
      // Add other domains if your application uses images from multiple sources
    },
  };
  
  module.exports = nextConfig;
  